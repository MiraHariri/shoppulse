/**
 * Theme exports for ShopPulse Analytics Design System
 */

export { theme, colors, typography, spacing, breakpoints, borderRadius, shadows, transitions, zIndex } from './design-tokens';
export type { Theme } from './design-tokens';
export { ThemeProvider, useTheme } from './ThemeProvider';
